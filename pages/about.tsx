import Head from "next/head";

const About = ({ }) => {
    return (
        <>
            <Head>
                <title>Rik den Breejen | About</title>
            </Head>
        </>
    );
};

export default About;